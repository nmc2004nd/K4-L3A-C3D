# Individual contribution report

Mỗi thành viên copy template này thành:

```text
reports/<student-id>-<short-name>.md
```

Giới hạn khuyến nghị: 1 trang, không chép lại README hoặc mô tả lý thuyết chung. Báo cáo không phải một bài pipeline cá nhân; mục đích là ghi nhận ownership và bằng chứng đóng góp trong sản phẩm nhóm.

---

## Thông tin

- Họ và tên: Hoàng Thái Đạt
- Mã học viên: 2A202602959
- Nhóm: C3D
- Repository/branch: https://github.com/nmc2004nd/K4-L3A-C3D/tree/hoangthaidat

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Chuẩn hóa dữ liệu | Clean và chuẩn hóa các file markdown dữ liệu (pháp lý và news) | `b146d04` | Done |
| Hoàn thiện Chatbot UI | Tích hợp `generate_with_citation()` vào `app.py`, hiển thị sources, lưu history session state | `d47927d` / `app.py` | Done |
| Đánh giá mô hình | Viết script `run_evaluation.py`, chạy benchmark, tổng hợp `eval_results.json`, cập nhật `RESULT.md` | `d47927d` | Done |
| Tinh chỉnh pipeline RAG | Sửa `SYSTEM_PROMPT` trong generation, tinh chỉnh tham số `CHUNK_SIZE` và `CHUNK_OVERLAP` | `d47927d` / `src/task10_generation.py`, `src/task4_chunking_indexing.py` | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:**  Sửa system prompt
   **Lý do/evidence:** System prompt ban đầu khi nhóm Nhóm sử dụng thì khi hỏi câu "Định nghĩa du lịch theo Luật 2017" systempromt không nhận diện được định nghĩa và trả lại safe refusal
   **Trade-off:** System prompt bớt nghiêm ngặt hơn, có thể mô hình chọn nhầm chunks.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: "Định nghĩa du lịch theo Luật 2017", "Định nghĩa du lịch theo Luật năm 2017", "Định nghĩa du lịch theo năm 2017"
- Kết quả trước/sau nếu có: Kết quả trước khi sửa system prompt là safe refusal
- Lỗi đã phát hiện và cách xử lý:Systemprompt quá chặt và có chút sai logic của nhóm.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Sử dụng recursive Chunking nên tài liệu chunking chưa thực sự tốt, ảnh hưởng đến khả năng tìm kiếm thông tin của RAG.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Thử nghiệm các phương pháp chunking khác như: Sentence Splitting, Paragraph Splitting.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày:20/09/2026
- Tên thành viên: Hoàng Thái Đạt
